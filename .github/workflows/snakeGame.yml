name: Snake Game

on:
  # run automatically every 24 hours
  schedule:
    - cron: "0 */24 * * *" 

  push:
    branches:
      - main
  pull_request:
    branches:
      - main

  # allows to manually run the job at any time
  workflow_dispatch:
  
jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v3
      
      - name: generate svgs
        uses: Platane/snk@v3
        with:
          github_user_name: resenkkomj
      
          # list of files to generate.
          # one file per line. Each output can be customized with options as query string.
          #
          #  supported options:
          #  - palette:     A preset of color, one of [github, github-dark, github-light]
          #  - color_snake: Color of the snake
          #  - color_dots:  Coma separated list of dots color.
          #                 The first one is 0 contribution, then it goes from the low contribution to the highest.
          #                 Exactly 5 colors are expected.
          outputs: |
            dist/github-snake-dark.svg?color_snake=#c7c7c7&color_dots=#161b22,#2c394d,#415674,#57729a,#6d8fc1
            dist/github-snake-light.svg?color_snake=#40c463&color_dots=#ebedf0,#c5d2e6,#a7bcda,#8aa5cd,#6d8fc1
        env:
          # a github token is required to fetch the contribution calendar from github API
          GITHUB_TOKEN: ${{ secrets.GH_ACTION_TOKEN }}

      - name: push svgs to the output branch
        uses: crazy-max/ghaction-github-pages@v3.1.0
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GH_ACTION_TOKEN }}
